function_commands:
  - function: my_system.functions.create_user
    name: new_user
    help_text: Create new user
    options:
      --name:
        required: true
      --password:
        required: true
help_text: This is the {project_name} interactive shell
project_name: Functions as Commands
shell:
  auto_import:
    display: true
    objects: {}
  banner:
    enabled: true
    message: Manage Interactive Shell! edit your `manage.yml`
  init_script: print("Starting interactive shell!")
  readline_enabled: true
